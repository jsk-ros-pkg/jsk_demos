cmake_minimum_required(VERSION 3.0.2)
project(jsk_spot_patrol)

find_package(catkin REQUIRED actionlib_msgs std_msgs
  )

# generate_messages(
#   DEPENDENCIES
#   std_msgs
#   actionlib_msgs
#   )

catkin_package(
  )
