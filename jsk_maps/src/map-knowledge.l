;;
;; knowledge base for jsk_maps
;; Commit candidate of irteus/irtscene.l
;;

;; +--building
;;   +--floor
;;     +--room

; room-definition
(defmethod scene-model
  (:regions (&optional name)
			;;(eq (class o) body)
	))